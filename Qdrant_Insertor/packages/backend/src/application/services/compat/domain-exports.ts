// Minimal compatibility shim: re-export domain services from the domain layer
// Keep this file tiny so we can later remove/replace with real application implementations.
export * from '../../../domain/services/index.js';
